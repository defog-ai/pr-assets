# pr-assets

Public image assets embedded in PR descriptions for defog-ai private repos.

Private repos cannot render images committed to the repo or referenced via blob URLs in PR bodies (GitHub's camo proxy cannot read private content). Images are hosted here (public) and referenced from PRs via their raw.githubusercontent.com URL.

Layout: `<repo-name>/<branch-or-topic>.png`
